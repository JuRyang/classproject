<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aia.op.member.dao.MemberDao"><!--MemberDao의 풀네임으로 별칭 정해야 한다. -->

	<!--result 매핑 -->
	<resultMap id="memberResult"
		type="com.aia.op.member.domain.Member"> <!--type="com.aia.op.member.domain.Member"객체로 쿼리 샐행 결과값을 받고자 함 -->
		<id column="idx" property="idx" />
		<result column="memberid" property="memberid" /> <!--column은 DB컬럼명  -->
		<result column="membername" property="membername" />
		<result column="password" property="password" />
		<result column="memberphoto" property="memberphoto" />
		<result column="regdate" property="regdate" />

	</resultMap>


	<insert id="insertMember"
		parameterType="com.aia.op.member.domain.Member">
		INSERT INTO member(memberid, password, membername,
		memberphoto)
		VALUES(#{memberid},#{password},#{membername},#{memberphoto})

	</insert>

	<select id="selectLogin" resultMap="memberResult">
		select * from member where memberid=#{param1} and password=#{param2}

	</select>








</mapper>